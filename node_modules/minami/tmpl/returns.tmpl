<?js
var data = obj || {};
if (data.description) {
?>
<div class="param-desc">
    <?js= description ?>
</div>
<?js } ?>

<?js if (data.type && data.type.names) {?>
<dl class="param-type">
    <dt>
        Type
    </dt>
    <dd>
        <?js= this.partial('type.tmpl', data.type.names) ?>
    </dd>
</dl>
<?js } ?>
<p>Summary se utilizara unicamente para que el usuario introduzca un breve resumen esplicando en que consiste y para que sirve este metodo</p>
<p>Ejemplo: getPhone(): bla bla bla ...</p>